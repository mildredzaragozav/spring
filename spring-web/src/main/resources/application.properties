spring.application.name=spring-web

# H2
spring.h2.console.enabled=true

# JPA ?
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.defer-datasource-initialization=true  # ? clave

# SQL init
spring.sql.init.mode=always