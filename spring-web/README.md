# Spring Web MVC Demo

## Project overview

This project shows how to build a REST API with Spring Web, it uses an H2 in memory database for persistence. Once the 
application is running, a script (data.sql) is executed to save data in the database, no additional database configuration is required.

The application is available at: http://localhost:8080/api/v1/models